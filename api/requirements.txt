# Floors rather than exact pins: the container builds on python:3.12-slim,
# but the same file has to install on whatever a developer has locally.
# The image is the reproducible artefact, not this file.
fastapi>=0.118
uvicorn[standard]>=0.37
pydantic-settings>=2.7
PyJWT>=2.10
argon2-cffi>=23.1
python-multipart>=0.0.20
filelock>=3.16
Pillow>=11.3
